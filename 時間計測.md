# 時間計測機能 仕様書

> **ステータス**: 設計段階（2026-03-18 作成）
> **目的**: cost-appの時間計測機能を拡張し、工程別タイマー・実質利益率計算・時間分析を実現する

---

## 1. 概要

### 背景

現在のcost-appには基本的なストップウォッチ機能（`/timetrack`）が実装されており、計測した時間を商品登録の`baseManHours`に反映できる。しかし、以下が不足している：

- **「何の作業か」を構造化する仕組みがない** — 自由入力のタスク名のみで、工程（裁断・縫製・検品等）の概念がない
- **実際の作業時間に基づく利益率がわからない** — 原価計算の人件費は手入力の見積もり時間であり、実績と乖離する
- **商品×工程の時間分析ができない** — どの商品のどの工程に時間がかかっているか可視化できない

### ゴール

| 優先度 | 機能 | 概要 |
|--------|------|------|
| **必須** | 工程別ワンタップタイマー | 商品と工程を選んでワンタップで計測開始 |
| **必須** | 時給設定→実質利益率 | 工程別時給 × 実績時間で「本当の利益率」を算出 |
| **連携** | 原価への人件費加算 | 計測データから人件費を原価計算に自動反映 |
| **連携** | マニュアルアプリ紐付け | 工程マスタを将来のマニュアルアプリと共有（構想） |
| **オプション** | 時間分析ダッシュボード | 商品別・工程別の時間可視化 |
| **オプション** | 目標時給アラート | 実質時給が目標を下回ったら警告 |

---

## 2. 現状整理

### 2.1 既存の時間計測機能

| 項目 | 現状 |
|------|------|
| **UI** | `/timetrack` ページにストップウォッチ（開始/一時停止/再開/ラップ/停止） |
| **データ** | `TimeRecord { id, taskName, totalDuration(ms), laps[{id, label, duration}], note? }` |
| **保存先** | Supabase `time_records` テーブル |
| **連携** | 商品登録の `baseManHours` に過去の計測結果をドロップダウンから選択→自動入力 |

### 2.2 既存の人件費計算

| 項目 | 現状 |
|------|------|
| **マスタ** | `LaborRole { id, name, hourlyRate, currency }` — 職種（≒「誰が」やるか） |
| **コストエントリ** | `LaborCostEntry { productId, laborRoleId, hours, peopleCount, hourlyRateOverride? }` |
| **計算式** | `人件費 = hourlyRate × hours × peopleCount` |
| **原価合計** | 材料+包装+人件費+外注+試作+設備+物流+電気+手数料 = 原価合計 |

### 2.3 既存の利益率計算

```
粗利 = 販売価格 - 原価合計
粗利率 = 粗利 / 販売価格 × 100
```

**課題**: `hours`は手入力の見積もり値。実際にかかった時間は反映されない。

---

## 3. 必須機能

### 3.1 工程モデル

#### 設計方針

- **LaborRole（職種）** = 「誰が」やるか → 既存のまま維持
- **工程（Process）** = 「何を」やるか → **新規追加**
- 商品ごとに工程を自由定義できる（ある商品は3工程、別の商品は8工程など）
- よく使う工程はテンプレートとして再利用可能
- **大工程・小工程の2階層**: 大工程（例: 縫製）の中に小工程（例: 本縫い、まつり縫い）を持てる
- **双方向の工程作成フロー**:
  - **工程→タイマー**: 既に定義された工程を選んで計測開始
  - **タイマー→工程**: フリータイマーで計測し、ラップを後から工程として登録

#### データモデル

**工程テンプレート（マスタ）** — よく使う工程名のプリセット（大工程・小工程の2階層）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `id` | UUID | PK |
| `parentId` | UUID? | FK → 自テーブル（NULLなら大工程、値ありなら小工程） |
| `name` | string | 工程名（大工程例: 縫製 / 小工程例: 本縫い） |
| `defaultHourlyRate` | number | デフォルト時給（円） |
| `color` | string? | UI表示色（タイマーボタン識別用） |
| `icon` | string? | アイコン名（Lucide） |
| `sortOrder` | number | 表示順 |
| `userId` | UUID | FK → auth.users |

```sql
-- process_templates テーブル（大工程・小工程の2階層）
CREATE TABLE process_templates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id),
  parent_id UUID REFERENCES process_templates(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  default_hourly_rate NUMERIC NOT NULL DEFAULT 0,
  color TEXT,
  icon TEXT,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
-- parent_id IS NULL → 大工程、parent_id IS NOT NULL → 小工程
```

**商品別工程** — 各商品に紐づく具体的な工程定義（大工程・小工程の2階層）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `id` | UUID | PK |
| `productId` | UUID | FK → products |
| `parentId` | UUID? | FK → 自テーブル（NULLなら大工程、値ありなら小工程） |
| `processTemplateId` | UUID? | FK → process_templates（テンプレートから作成時） |
| `name` | string | 工程名（テンプレートから引き継ぎ or カスタム） |
| `hourlyRate` | number | この工程の時給（テンプレートのデフォルトを上書き可） |
| `estimatedMinutes` | number? | 見積もり時間（分） |
| `sortOrder` | number | 工程の実行順序 |
| `userId` | UUID | FK → auth.users |

```sql
-- product_processes テーブル（大工程・小工程の2階層）
CREATE TABLE product_processes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id),
  product_id UUID NOT NULL REFERENCES products(id) ON DELETE CASCADE,
  parent_id UUID REFERENCES product_processes(id) ON DELETE CASCADE,
  process_template_id UUID REFERENCES process_templates(id) ON DELETE SET NULL,
  name TEXT NOT NULL,
  hourly_rate NUMERIC NOT NULL DEFAULT 0,
  estimated_minutes NUMERIC,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
-- parent_id IS NULL → 大工程、parent_id IS NOT NULL → 小工程
```

#### 工程の設定フロー

**フローA: 工程→タイマー（事前定義型）**
```
[マスタ登録] 工程テンプレートを登録（裁断, 縫製(→本縫い,まつり縫い), 検品, 梱包...）
     ↓
[商品登録] 商品にテンプレートから工程を追加 or カスタム工程を自由追加
     ↓  各工程に時給を設定（テンプレートのデフォルト or 個別設定）
     ↓
[タイマー] 商品を選ぶ → その商品の工程一覧が表示 → タップで計測
```

**フローB: タイマー→工程（計測から定義型）**
```
[タイマー] フリータイマーで計測開始（商品のみ選択、工程は未指定）
     ↓  作業の区切りでラップを記録
     ↓
[計測完了] ラップ一覧が表示される
     ↓
[工程化] 各ラップを工程として登録:
     ├─ 「ラップ1 (12:30)」→ 大工程「裁断」として登録
     ├─ 「ラップ2 (45:00)」→ 大工程「縫製」として登録
     │   └─ または → 小工程に分割:「本縫い」「まつり縫い」
     └─ 「ラップ3 (10:15)」→ 大工程「検品」として登録
     ↓
[テンプレート化] 「この工程セットをテンプレートとして保存しますか？」
     → 次回からフローAで使用可能に
```

**フローBの詳細: ラップから工程を作成するUI**

```
┌─────────────────────────────────────────────┐
│ 計測完了 — ラップを工程に変換               │
│                                              │
│ 商品: トートバッグ Mサイズ                   │
│                                              │
│ ┌──────────────────────────────────────────┐ │
│ │ ラップ1  12:30  → [工程名: 裁断    ▼]   │ │
│ │                    [大工程 ○] [小工程 ○] │ │
│ │                    [時給: ¥1,200       ] │ │
│ ├──────────────────────────────────────────┤ │
│ │ ラップ2  45:00  → [工程名: 縫製    ▼]   │ │
│ │                    [大工程 ●] [小工程 ○] │ │
│ │                    [時給: ¥1,500       ] │ │
│ ├──────────────────────────────────────────┤ │
│ │ ラップ3  10:15  → [工程名: 検品    ▼]   │ │
│ │                    [大工程 ●] [小工程 ○] │ │
│ │                    [時給: ¥1,000       ] │ │
│ ├──────────────────────────────────────────┤ │
│ │ ラップ4   8:20  → [工程名: 梱包    ▼]   │ │
│ │                    [大工程 ●] [小工程 ○] │ │
│ │                    [時給: ¥1,000       ] │ │
│ └──────────────────────────────────────────┘ │
│                                              │
│ ☐ この工程セットをテンプレートとして保存     │
│                                              │
│ [スキップ（工程なしで保存）]  [工程として登録] │
└─────────────────────────────────────────────┘
```

> **ポイント**: フリータイマーで試しに計測し、慣れてきたら工程として構造化する、という段階的な導入が可能。新商品の初回製作時にフローBで工程を定義し、2回目以降はフローAで効率的に計測する使い方を想定。

### 3.2 工程別ワンタップタイマー

#### UI構成

```
┌─────────────────────────────────────────┐
│ ← 時間計測                              │
├─────────────────────────────────────────┤
│                                         │
│  ■ 商品選択                             │
│  ┌─────────────────────────────────┐    │
│  │ ▼ トートバッグ Mサイズ           │    │
│  └─────────────────────────────────┘    │
│                                         │
│  ■ 工程（ワンタップで計測開始）         │
│  ┌──────────┐ ┌──────────────────┐     │
│  │ ✂ 裁断   │ │ 🧵 縫製          │     │
│  │ ¥1,200/h │ │ ¥1,500/h         │     │
│  └──────────┘ │  ├ 本縫い         │     │
│               │  ├ まつり縫い      │     │
│               │  └ ボタン付け      │     │
│               └──────────────────┘     │
│  ┌──────────┐ ┌──────────┐             │
│  │ 🔍 検品  │ │ 📦 梱包  │             │
│  │ ¥1,000/h │ │ ¥1,000/h │             │
│  └──────────┘ └──────────┘             │
│                                         │
│  ※ 大工程タップ → 大工程全体を計測      │
│  ※ 小工程タップ → 小工程単位で計測      │
│                                         │
│  ── または ──                           │
│                                         │
│  [フリータイマー（工程なし）]            │
│                                         │
├─────────────────────────────────────────┤
│                                         │
│         00:12:34.56                     │
│         縫製 - トートバッグ Mサイズ      │
│                                         │
│    [ラップ]  [一時停止]  [停止]          │
│                                         │
├─────────────────────────────────────────┤
│  ■ 記録履歴                             │
│  ┌───────────────────────────────────┐  │
│  │ 日付    商品      工程   時間     │  │
│  │ 03/18  トートM   縫製  0:45:12   │  │
│  │ 03/18  トートM   裁断  0:12:30   │  │
│  │ 03/17  ポーチS   縫製  0:30:05   │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

#### 操作フロー

**パターン1: 工程指定で計測（フローA）**
1. **商品選択**（ドロップダウン）
2. **大工程 or 小工程タップ** → 即座にタイマー開始（ワンタップ）
3. **計測中**: ラップ / 一時停止 / 停止
4. **停止時**: 自動保存（商品+工程が紐付いた状態で記録）

**パターン2: フリー計測→工程化（フローB）**
1. **商品選択**（ドロップダウン） — 省略も可
2. **フリータイマー開始**（工程未指定）
3. **計測中**: 作業の切り替わりでラップを記録
4. **停止時**: ラップ→工程変換UIを表示
   - 各ラップに工程名を割り当て（既存テンプレートから選択 or 新規入力）
   - 大工程/小工程を指定
   - 「テンプレートとして保存」オプション
   - スキップして工程なしで保存も可能

**共通**:
5. **記録履歴**: 商品名・工程名（大/小）・時間を一覧表示、編集・削除可能

#### TimeRecord の拡張

| フィールド | 型 | 変更 | 説明 |
|-----------|-----|------|------|
| `id` | UUID | 既存 | |
| `taskName` | string | 既存 | 後方互換のため残す。工程名を自動セット |
| `totalDuration` | number | 既存 | ミリ秒 |
| `laps` | LapData[] | 既存 | |
| `note` | string? | 既存 | |
| `productId` | UUID? | **新規** | FK → products（任意） |
| `productProcessId` | UUID? | **新規** | FK → product_processes（任意） |
| `createdAt` | string | 既存 | |
| `updatedAt` | string | 既存 | |

```sql
-- time_records テーブルへのカラム追加
ALTER TABLE time_records
  ADD COLUMN product_id UUID REFERENCES products(id) ON DELETE SET NULL,
  ADD COLUMN product_process_id UUID REFERENCES product_processes(id) ON DELETE SET NULL;
```

### 3.3 時給設定と実質利益率

#### 時給の設定箇所

| 設定箇所 | 用途 | 優先度 |
|---------|------|--------|
| 工程テンプレート `defaultHourlyRate` | マスタのデフォルト値 | 低（フォールバック） |
| 商品別工程 `hourlyRate` | 商品×工程ごとの時給 | **高（実際に使用）** |

#### 計算式

**従来の粗利率（見積もりベース）**:

```
見積もり人件費 = Σ(LaborCostEntry: hourlyRate × hours × peopleCount)
粗利率 = (販売価格 - 原価合計) / 販売価格 × 100
```

**実質利益率（実績ベース）** — **新規追加**:

```
実績人件費(商品) = Σ(商品の各工程について:
    工程の平均実績時間(時間) × 工程の時給
)

工程の平均実績時間 = 該当工程のTimeRecord.totalDuration の平均 / 3,600,000

実質原価 = 原価合計(人件費除く) + 実績人件費
実質利益率 = (販売価格 - 実質原価) / 販売価格 × 100
```

#### 計算例

| 項目 | 値 |
|------|-----|
| 販売価格 | ¥5,000 |
| 材料+包装+その他原価（人件費除く） | ¥1,200 |
| **見積もり人件費**（手入力: 1.5H × ¥1,200） | ¥1,800 |
| **実績人件費**（実測: 縫製45分×¥1,500 + 裁断15分×¥1,200 + 検品10分×¥1,000 + 梱包8分×¥1,000） | **¥1,725** |

```
見積もり粗利率 = (5,000 - 1,200 - 1,800) / 5,000 × 100 = 40.0%
実質利益率     = (5,000 - 1,200 - 1,725) / 5,000 × 100 = 41.5%
```

#### 表示場所

| 場所 | 表示内容 |
|------|---------|
| **原価サマリ（/cost）** | 見積もり粗利率と実質利益率を並列表示。差分をハイライト |
| **商品一覧（/list）** | 実質利益率カラムを追加（データがある商品のみ） |
| **集計データ（/analytics）** | 見積もり vs 実績の乖離グラフ |

---

## 4. 連携機能

### 4.1 原価計算への人件費自動加算

#### 現状のフロー（手動）

```
[タイマーで計測] → [baseManHours に手動反映] → [LaborCostEntry の hours に使用]
```

#### 新フロー（自動連携）

```
[タイマーで計測] → TimeRecord に productId + productProcessId 付きで保存
      ↓
[原価サマリ表示時] calculateProductUnitCosts() が:
      ↓
  1. 従来のLaborCostEntry から「見積もり人件費」を算出（既存ロジック維持）
  2. TimeRecord から「実績人件費」を算出（新規ロジック追加）
  3. 両方を返却 → UIで並列表示
```

#### calculateProductUnitCosts の拡張案

```typescript
// 戻り値に追加
type ProductUnitCosts = {
  // ... 既存の9カテゴリ + total ...

  // 新規: 実績ベースの人件費
  actualLabor: number          // 実績人件費合計
  actualLaborByProcess: {      // 工程別内訳
    processId: string
    processName: string
    avgMinutes: number         // 平均実績時間（分）
    hourlyRate: number         // 工程時給
    cost: number               // avgMinutes/60 × hourlyRate
    recordCount: number        // 計測回数（信頼度の指標）
  }[]

  // 新規: 実質原価・実質利益率
  actualTotal: number          // 原価合計(人件費除く) + 実績人件費
  effectiveProfitRate: number  // 実質利益率 (%)
}
```

#### 「実績人件費で原価を上書き」オプション

ユーザーが十分な計測データを蓄積した後、見積もり人件費の代わりに実績人件費を原価として採用するオプションを想定：

| モード | 原価の人件費 | 用途 |
|--------|------------|------|
| **見積もりモード**（デフォルト） | LaborCostEntry の手入力値 | 新商品、計測データ不足時 |
| **実績モード** | TimeRecord の平均実績値 | 十分な計測データがある商品 |

切り替えは商品ごとに設定可能とする（`products.laborCostMode: 'estimate' | 'actual'`）。

### 4.2 マニュアルアプリとの連携（構想）

> **注**: マニュアルアプリは構想段階のため、ここでは連携の方向性とインターフェース契約のみ定義する。

#### 想定する連携内容

| 連携項目 | 方向 | 内容 |
|---------|------|------|
| 工程マスタの共有 | cost-app ↔ マニュアルアプリ | 同じ工程名・IDを両アプリで参照 |
| 工程の手順情報 | マニュアルアプリ → cost-app | タイマー画面で「この工程のマニュアルを見る」リンク |
| 実績時間の参照 | cost-app → マニュアルアプリ | マニュアル側で「この工程の平均所要時間」を表示 |

#### インターフェース契約（案）

```typescript
// 工程マスタの共有フォーマット（将来のAPI/エクスポート用）
type SharedProcess = {
  id: string                  // UUID（両アプリ共通）
  name: string                // 工程名
  description?: string        // 工程の説明
  estimatedMinutes?: number   // 見積もり時間
  avgActualMinutes?: number   // 実績平均時間（cost-app側で算出）
}
```

#### 連携方式の候補

| 方式 | メリット | デメリット |
|------|---------|-----------|
| **共有Supabaseテーブル** | リアルタイム同期、実装シンプル | 両アプリが同じDBに依存 |
| **REST API** | 疎結合、独立デプロイ | API開発・認証の追加コスト |
| **CSV/JSONエクスポート** | 最小実装、オフライン対応 | 手動同期、リアルタイム性なし |

**推奨**: まずはcost-app側で`process_templates`を整備し、マニュアルアプリ着手時に共有方式を決定する。`process_templates.id`をUUIDで管理しておけば、どの連携方式でも対応可能。

---

## 5. オプション機能

### 5.1 商品別・工程別の時間分析

#### ダッシュボード画面（集計データタブ内に追加）

```
┌─────────────────────────────────────────────────┐
│ ■ 時間分析                                       │
│                                                   │
│ [商品フィルター ▼] [期間: 過去30日 ▼]            │
│                                                   │
│ ── 商品別 合計作業時間 ──                         │
│ トートバッグ M   ████████████████  1:18:00 (5個)  │
│ ポーチ S        ████████          0:42:00 (8個)  │
│ リュック L      ████████████████████ 2:05:00 (2個)│
│                                                   │
│ ── トートバッグ M: 工程別内訳 ──                   │
│ ┌─────────┬──────────┬──────────┬───────┐        │
│ │ 工程    │ 平均時間  │ 時給     │ 人件費 │        │
│ ├─────────┼──────────┼──────────┼───────┤        │
│ │ 裁断    │ 12分30秒  │ ¥1,200  │ ¥250  │        │
│ │ 縫製    │ 45分00秒  │ ¥1,500  │ ¥1,125│        │
│ │ 検品    │ 10分15秒  │ ¥1,000  │ ¥171  │        │
│ │ 梱包    │  8分20秒  │ ¥1,000  │ ¥139  │        │
│ ├─────────┼──────────┼──────────┼───────┤        │
│ │ 合計    │ 1:16:05  │    —    │ ¥1,685│        │
│ └─────────┴──────────┴──────────┴───────┘        │
│                                                   │
│ ── 工程別 時間推移（折れ線グラフ）──               │
│  45m ┤         ╭─╮                               │
│  30m ┤    ╭──╮╯   ╰──╮                          │
│  15m ┤╭──╯               ╰──                     │
│      └──┬──┬──┬──┬──┬──┬──                       │
│       3/12 13 14 15 16 17 18                      │
│       ● 縫製  ● 裁断  ● 検品                     │
│                                                   │
│ ── 見積もり vs 実績 乖離 ──                       │
│ ┌─────────┬──────────┬──────────┬───────┐        │
│ │ 商品    │ 見積もり  │ 実績     │ 乖離  │        │
│ ├─────────┼──────────┼──────────┼───────┤        │
│ │ トートM │ ¥1,800   │ ¥1,685  │ -6.4% │        │
│ │ ポーチS │ ¥800     │ ¥950   │+18.8% │        │
│ └─────────┴──────────┴──────────┴───────┘        │
└─────────────────────────────────────────────────┘
```

#### 分析指標

| 指標 | 計算式 | 用途 |
|------|--------|------|
| 工程別平均時間 | `Σ(該当工程のtotalDuration) / 記録数` | 標準時間の把握 |
| 工程別人件費 | `平均時間(H) × 工程時給` | コスト配分の可視化 |
| 見積もり乖離率 | `(実績 - 見積もり) / 見積もり × 100` | 見積もり精度の改善 |
| 1個あたり合計時間 | `Σ(全工程の平均時間)` | 生産性指標 |
| 時間推移 | 日別/週別の工程時間プロット | 習熟度・効率の可視化 |

### 5.2 目標時給アラート

#### 概念

「この商品を作って販売したとき、自分の時給は目標に達しているか？」を可視化する。

#### 計算式

```
実質時給 = (販売価格 - 原価合計(人件費除く)) / 実績合計時間(H)

例:
  販売価格: ¥5,000
  人件費除く原価: ¥1,200
  実績合計時間: 1.27H（76分）
  → 実質時給 = (5,000 - 1,200) / 1.27 = ¥2,992/h
```

#### 設定

| 設定項目 | 型 | 説明 |
|---------|-----|------|
| `targetHourlyRate` | number | 目標時給（円）。ユーザー設定画面で1つ設定 |
| `alertEnabled` | boolean | アラートのON/OFF |

#### アラート表示

| 条件 | 表示 |
|------|------|
| 実質時給 ≥ 目標時給 | 🟢 緑バッジ（目標達成） |
| 実質時給 ≥ 目標時給 × 0.8 | 🟡 黄バッジ（やや注意） |
| 実質時給 < 目標時給 × 0.8 | 🔴 赤バッジ（要改善） |

**表示箇所**:
- 商品一覧の各行に時給バッジ
- 原価サマリに実質時給セクション
- （オプション）タイマー停止時に即時フィードバック

---

## 6. データモデル全体図

```
┌──────────────────────┐     ┌─────────────────────┐
│ process_templates    │     │ products             │
│ (工程テンプレート)    │     │ (商品)               │
│──────────────────────│     │─────────────────────│
│ id                   │     │ id                   │
│ parentId (→自テーブル)│     │ name                 │
│ name                 │◄─┐  │ salePrice            │
│ defaultHourlyRate    │  │  │ laborCostMode        │ ← 新規: 'estimate'|'actual'
│ color                │  │  │ ...                  │
│ icon                 │  │  └──────────┬──────────┘
│ sortOrder            │  │             │
└──────────────────────┘  │             │ 1:N
  ↑ parentId で階層化     │  ┌──────────▼──────────┐
  大工程 (parentId=NULL)   │  │ product_processes    │
    └ 小工程 (parentId=大) │  │ (商品別工程)          │
                           │  │─────────────────────│
                           └──│ processTemplateId    │
                              │ productId            │
                              │ parentId (→自テーブル)│ ← 大工程/小工程の階層
                              │ name                 │
                              │ hourlyRate           │
                              │ estimatedMinutes     │
                              │ sortOrder            │
                              └──────────┬──────────┘
                                ↑ parentId で階層化   │
                                大工程 (parentId=NULL) │ 1:N
                                  └ 小工程             │
                              ┌──────────▼──────────┐
                              │ time_records         │
                              │ (計測記録) ※拡張     │
                              │─────────────────────│
                              │ id                   │
                              │ taskName             │
                              │ totalDuration        │
                              │ laps                 │
                              │ note                 │
                              │ productId            │ ← 新規
                              │ productProcessId     │ ← 新規（大 or 小工程）
                              └─────────────────────┘

双方向フロー:
  フローA: process_templates → product_processes → タイマー → time_records
  フローB: タイマー → time_records(ラップ) → product_processes に変換 → process_templates に保存
```

---

## 7. 画面遷移

```
[サイドバー]
  ├─ 原価サマリ (/cost) ............. 実質利益率を追加表示
  ├─ 集計データ (/analytics) ........ 時間分析セクション追加
  ├─ 商品登録 (/product) ............ 工程定義セクション追加
  ├─ マスタ登録 (/master) ........... 工程テンプレート管理を追加
  ├─ 商品一覧 (/list) .............. 実質利益率・実質時給カラム追加
  └─ 時間計測 (/timetrack) ......... ワンタップタイマーに大幅改修
       ├─ 商品選択
       ├─ 工程ボタン群（ワンタップ開始）
       ├─ ストップウォッチ（既存を維持）
       └─ 記録履歴（商品・工程カラム追加）
```

### マスタ登録への追加

工程テンプレートを既存のマスタ登録タブ内に追加する：

```
[マスタ登録]
  ├─ カテゴリ
  ├─ 材料
  ├─ 包装資材
  ├─ 配送方法
  ├─ 職種・時給（既存 LaborRole）
  ├─ 工程テンプレート            ← 新規追加
  ├─ 設備
  ├─ 手数料
  └─ ...
```

### 商品登録への追加

商品フォーム内に「工程定義」セクションを追加：

```
[商品登録フォーム]
  ├─ 基本情報
  ├─ 生産設定
  ├─ 工程定義                   ← 新規追加
  │   ├─ [テンプレートから追加 ▼]
  │   ├─ [カスタム大工程を追加]
  │   └─ ドラッグ&ドロップで順序変更
  │       ┌─────────┬──────────────┬──────────┬──────────┐
  │       │ 順序    │ 工程名       │ 時給     │ 見積時間  │
  │       ├─────────┼──────────────┼──────────┼──────────┤
  │       │ 1 ≡    │ 裁断         │ ¥1,200  │ 15分     │
  │       │ 2 ≡    │ 縫製         │ ¥1,500  │ 45分     │
  │       │   2-1  │  └ 本縫い     │ ¥1,500  │ 30分     │
  │       │   2-2  │  └ まつり縫い │ ¥1,500  │ 10分     │
  │       │   2-3  │  └ ボタン付け │ ¥1,200  │  5分     │
  │       │ 3 ≡    │ 検品         │ ¥1,000  │ 10分     │
  │       │ 4 ≡    │ 梱包         │ ¥1,000  │ 10分     │
  │       └─────────┴──────────────┴──────────┴──────────┘
  │   ※ 大工程に [+小工程] ボタンで子工程を追加可能
  │   ※ 小工程の時給は大工程から継承（個別上書き可）
  ├─ 材料コスト
  ├─ 包装コスト
  ├─ 人件費コスト（既存）
  └─ ...
```

---

## 8. 計算式まとめ

| 計算 | 式 | 備考 |
|------|-----|------|
| **見積もり人件費** | `Σ(hourlyRate × hours × peopleCount)` | 既存。LaborCostEntry ベース |
| **実績人件費** | `Σ(工程平均時間(H) × 工程時給)` | 新規。TimeRecord + ProductProcess ベース |
| **工程平均時間** | `avg(totalDuration) / 3,600,000` | 該当 productProcessId の全レコード |
| **粗利率（見積もり）** | `(販売価格 - 原価合計) / 販売価格 × 100` | 既存 |
| **実質利益率** | `(販売価格 - 原価(人件費除く) - 実績人件費) / 販売価格 × 100` | 新規 |
| **実質時給** | `(販売価格 - 原価(人件費除く)) / 実績合計時間(H)` | 新規。アラート判定に使用 |
| **見積もり乖離率** | `(実績人件費 - 見積もり人件費) / 見積もり人件費 × 100` | 新規。分析用 |

---

## 9. 実装優先度

### Phase 1: 基盤（必須）

| # | タスク | 依存 |
|---|--------|------|
| 1-1 | `process_templates` テーブル作成 + マスタ登録UI | — |
| 1-2 | `product_processes` テーブル作成 + 商品登録の工程定義UI | 1-1 |
| 1-3 | `time_records` に `productId`, `productProcessId` カラム追加 | 1-2 |
| 1-4 | ワンタップタイマーUI（商品選択→工程タップ→計測） | 1-3 |
| 1-5 | 実質利益率の計算ロジック（`calculations.ts` 拡張） | 1-3 |
| 1-6 | 原価サマリに実質利益率を表示 | 1-5 |

### Phase 2: 連携

| # | タスク | 依存 |
|---|--------|------|
| 2-1 | 見積もりモード/実績モード切替（`laborCostMode`） | 1-5 |
| 2-2 | 商品一覧に実質利益率カラム追加 | 1-5 |
| 2-3 | マニュアルアプリ連携用の工程エクスポート機能 | 1-1 |

### Phase 3: 分析・アラート（オプション）

| # | タスク | 依存 |
|---|--------|------|
| 3-1 | 時間分析ダッシュボード（集計データタブ内） | 1-4 |
| 3-2 | 見積もり vs 実績 乖離分析 | 1-5 |
| 3-3 | 目標時給の設定UI | — |
| 3-4 | 目標時給アラート表示 | 3-3, 1-5 |
| 3-5 | 工程別時間推移グラフ | 3-1 |

---

## 10. 既存機能への影響

| 既存機能 | 影響 | 対応方針 |
|---------|------|---------|
| ストップウォッチ | UI大幅変更 | 工程選択UI追加。フリータイマーモードで後方互換維持 |
| 記録履歴 | カラム追加 | 商品名・工程名カラム追加。既存レコードは「—」表示 |
| baseManHours連携 | 拡張 | 従来の手動選択に加え、工程別実績からの自動提案を追加 |
| 原価計算 | ロジック追加 | 既存の計算は変更なし。実績ベースの計算を**追加**で返却 |
| LaborRole | 変更なし | 「誰が」の概念として維持。工程（「何を」）とは別軸 |
| Excel/PDF出力 | 項目追加 | 実質利益率・実質時給を出力項目に追加 |

---

## 11. 未決事項

| # | 項目 | 検討ポイント |
|---|------|------------|
| Q1 | 複数人作業の扱い | 1つの工程を2人で同時にやる場合、時間×2で計算するか？ |
| Q2 | バッチ生産の扱い | 10個まとめて裁断した場合、1個あたりの時間はどう算出するか？ |
| Q3 | 記録の最小信頼数 | 実績データが何件以上あれば「実質利益率」を表示するか？（案: 3件以上） |
| Q4 | 旧データの移行 | 既存の`taskName`ベースのTimeRecordを工程に紐付けるか？ |
| Q5 | オフライン対応 | タイマー使用中にネットワークが切れた場合のローカル保存 |
| Q6 | 大工程の時間集計 | 小工程が定義されている大工程の合計時間は、小工程の合算とするか、大工程単体の計測も許可するか？ |
| Q7 | ラップ→工程変換の粒度 | 1ラップ＝1大工程が基本。1ラップを複数の小工程に分割する操作は初期実装で必要か？ |
| Q8 | 工程テンプレートのセット保存 | フローBで作った工程セットを「テンプレートセット」として一括保存・一括適用する仕組みの詳細 |
