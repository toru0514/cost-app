# Issue #162 コンテンツエリア検討

## 確定事項
- 左メニューバー: 案A（展開時アイコン+テキスト、折りたたみ時アイコンのみ）
- モバイル: ハンバーガーメニュー

## 現在の構造

```
┌──────────────────────────────────────────────┐
│  header（タイトル、バッジ、ボタン群）          │
├──────────────────────────────────────────────┤
│  TabsList（タブ切り替え）                     │
├──────────────────────────────────────────────┤
│  Card                                        │
│  ────────────────────────────                │
│  Card                                        │
│  ────────────────────────────                │
│  Card                                        │
│  ────────────────────────────                │
│        ↓ 縦にスタック（max-w-6xl）            │
└──────────────────────────────────────────────┘
```

## 課題
1. コンテンツ幅が `max-w-6xl`（1152px）に制限されており、テーブルが横スクロールになりがち
2. カードが縦に並ぶだけで、画面の横幅を有効活用できていない

## 各ページのコンテンツ特性

| ページ | コンテンツ特性 | 最適幅 |
|--------|--------------|--------|
| 原価サマリ | サマリーカード群 + 詳細テーブル | カードは並列可、テーブルは広め |
| 集計データ | グラフ群 + フィルター | グラフは並列表示が効果的 |
| 商品登録 | 入力フォーム（多段階） | 適度な幅（全幅は逆に読みにくい） |
| マスタ登録 | カテゴリ別リスト/テーブル | テーブルは広め |
| 商品/在庫一覧 | 大きなテーブル（カラム多） | **最も幅が必要** |
| 一括処理 | ボタン群 + ログ | 狭くてOK |
| 監査ログ | テーブル | 広め |

## 検討案

### 案1: コンテンツタイプ別の幅最適化
- テーブル系（list, audit, cost）: 全幅
- フォーム系（product, master）: max-w-4xl〜5xl
- グラフ系（analytics）: グリッドレイアウト

### 案2: グリッドレイアウトの活用
- サマリーカードは2〜3カラムグリッド
- テーブルは全幅
- 関連情報をサイドバーに

### 案3: ダッシュボード型
- ウィジェット風のカード配置
- ドラッグ&ドロップで並び替え

### 案4: ページごとに幅を変える
- list/audit → 全幅
- product → max-w-4xl
- cost/analytics → グリッドレイアウト

---

## 検証履歴

### 検証1: セクションごとの幅最適化（2026-03-08）

**変更内容:**
- `/ui-prototype/` にセクションごとの幅設定を追加
- 幅設定:
  | セクション | 幅設定 | 理由 |
  |-----------|--------|------|
  | cost | max-w-6xl | サマリーカード + テーブル |
  | analytics | max-w-6xl | グラフ（将来グリッド化） |
  | product | max-w-4xl | 入力フォーム（狭め） |
  | master | max-w-5xl | リスト/テーブル |
  | list | max-w-full | 大きなテーブル（全幅） |
  | bulk | max-w-4xl | ボタン群（狭め） |
  | audit | max-w-full | テーブル（全幅） |

- サンプルテーブル（12カラム）を追加して全幅効果を確認可能に
- 商品登録フォームのサンプルを追加

**検証URL:**
- `/ui-prototype/list` - 全幅テーブル
- `/ui-prototype/product` - 狭めフォーム
- `/ui-prototype/audit` - 全幅テーブル

**検証観点:**
- テーブルの横スクロールが減るか
- 余白のバランスは適切か
- フォームは狭い方が読みやすいか
- セクションごとに幅が変わることに違和感はないか

---

### 検証2: テーブル中心デザイン（Airtable風）（2026-03-08）

**コンセプト:**
- カードを使わずテーブルを直接配置
- 行ホバーでアクションボタン表示
- シンプルなセクションヘッダー（見出し + 説明のみ）
- ツールバー型の検索・フィルター

**特徴:**
| 要素 | 従来（カード） | 新デザイン |
|------|-------------|-----------|
| テーブル | Card内に配置 | 直接配置（border-roundedのみ） |
| ヘッダー | CardTitle/CardDescription | シンプルなh1/p |
| アクション | 常に表示 | 行ホバーで表示（opacity制御） |
| 検索/フィルター | Card内 | ツールバーとしてテーブル上部に配置 |
| 統計 | 個別Card | シンプルなグリッド（border + padding） |

**実装内容:**
- `/ui-prototype/list` - テーブル中心の商品一覧
- `/ui-prototype/product` - カードなしフォーム
- `/ui-prototype/cost` - 統計グリッド + テーブル
- `/ui-prototype/bulk` - シンプルなアクションブロック

**検証観点:**
- カードがない方がスッキリ見えるか
- 行ホバーのアクション表示は直感的か
- 情報密度は適切か
