# Cost App 改善課題リスト

Cost App（原価管理アプリ）のコードベースを全体的に調査し、UI/UX・機能・アーキテクチャ・パフォーマンス・品質の観点から課題を洗い出した。

---

## 1. アーキテクチャ / コード構造

| # | 課題 | 詳細 |
|---|------|------|
| A1 | **dashboard-page.tsx が巨大すぎる** | 1ファイル58KB超。レイアウト・状態管理・全タブのロジックが集中しており、保守性・可読性が低い |
| A2 | **useAppData フックが巨大すぎる** | `app-data.ts` が1497行。全CRUDロジックが1つのフックに集約されている |
| A3 | **app-data-sync.ts が巨大** | 1145行。テーブルごとの同期処理が手続き的に並んでおり、テスト困難 |
| A4 | **サーバーコンポーネント未活用** | Next.js App Router を使用しているが、ほぼ全てクライアントコンポーネント。初期データのSSR/RSCによる取得がない |
| A5 | **API ルートの認証パターン不統一** | Bearer トークン取得やユーザー認証のロジックが各ルートで重複 |
| A6 | **型定義ファイルの肥大化** | `types.ts` が627行。ドメインごとに分割されていない |

## 2. UI/UX

| # | 課題 | 詳細 |
|---|------|------|
| U1 | **初回利用時のオンボーディングがない** | 新規ユーザーが何から始めればよいか分からない。チュートリアルやウィザードが未実装 |
| U2 | **空状態 (Empty State) の表示が不十分** | データ未登録時に「ここにデータが表示されます」のような案内がない箇所がある |
| U3 | **フィードバックがトーストのみ** | 操作結果の通知がトーストに依存。重要な操作（削除・一括変更）の確認UIが弱い |
| U4 | **ナビゲーションの深さが分かりにくい** | タブの階層が深い（マスタ登録内のサブセクション等）が、パンくずリストなどの現在位置表示がない |
| U5 | **検索結果のハイライトがない** | 検索しても該当箇所がハイライトされず、結果が分かりにくい |
| U6 | **キーボードショートカット未対応** | タブ切替・検索・保存などの頻繁操作にショートカットがない |
| U7 | **ダークモード切替UIがない** | CSSでダークモード対応はしているが、ユーザーが手動で切り替えるUIがない |
| U8 | **テーブルの一括操作がない** | 商品一覧でチェックボックス選択→一括削除/一括編集ができない |
| U9 | **フォームの入力補助が弱い** | 数値入力のフォーマット（カンマ区切り等）、単位表示、入力ヒントが不足 |
| U10 | **モバイルでの操作性** | テーブルの横スクロールは対応しているが、行アクション（編集・削除）がホバー依存でタッチデバイスで使いにくい |
| U11 | **ローディング表現の統一感がない** | スピナー、テキスト「読込中...」、ボタンの disabled 状態などが混在 |
| U12 | **エラー表示の体験が弱い** | ネットワークエラー時のリトライUI、エラー境界（Error Boundary）が未実装 |

## 3. 機能面

| # | 課題 | 詳細 |
|---|------|------|
| F1 | **Undo / Redo がない** | 操作の取り消しができない。特にマスタデータの編集・削除時に重要 |
| F2 | **データのバージョン管理が弱い** | バックアップ・ロールバック機能はあるが、変更履歴の差分表示がUIから確認しにくい |
| F3 | **複数ユーザー/チーム対応がない** | 現在シングルユーザー前提。RLSは設定済みだが、チームでの共同編集は未対応 |
| F4 | **商品のステータス管理がない** | 商品の「下書き」「販売中」「廃番」などのライフサイクル管理がない |
| F5 | **原価シミュレーション機能が限定的** | 「もし材料費がX%上がったら？」のような what-if 分析が弱い |
| F6 | **通知機能が Slack のみ** | アプリ内通知、メール通知がない。在庫アラートもSlack限定 |
| F7 | **レポート/帳票出力がない** | PDFやExcelでの原価表出力、見積書作成機能がない |
| F8 | **マスタデータの依存関係チェックが弱い** | 材料を削除する際、その材料を使用している商品への影響確認が参照カウント表示のみ |
| F9 | **通貨対応が不完全** | currency フィールドはあるが、為替レート管理や自動換算機能がない |
| F10 | **画像/ファイル添付がない** | 商品や材料に画像を紐づけられない |

## 4. パフォーマンス

| # | 課題 | 詳細 |
|---|------|------|
| P1 | **全データを一括ロード** | `loadUserAppData()` で21テーブルを並列フェッチ。データ量増加時にパフォーマンス劣化が予想される |
| P2 | **リスト表示の仮想化がない** | 商品や材料が多い場合、全件DOMレンダリング。React Virtualizeなどの仮想スクロール未導入 |
| P3 | **画像/アセットの最適化がない** | Next.js の Image コンポーネントが未使用の箇所がある可能性 |
| P4 | **バンドルサイズの最適化** | googleapis ライブラリ等のサーバーサイド専用パッケージがクライアントバンドルに含まれている可能性 |
| P5 | **データ保存の粒度が粗い** | 1項目変更でも全データを保存する。差分保存（部分更新）が未実装 |

## 5. テスト / 品質

| # | 課題 | 詳細 |
|---|------|------|
| T1 | **テストカバレッジが低い** | テストは bulk-sync 関連の7ファイルのみ。UI・フック・計算ロジックのテストがほぼない |
| T2 | **E2Eテストがない** | Playwright/Cypress などのブラウザテストが未導入 |
| T3 | **TypeScript strictモードの警告** | 一部 `any` 型やimplicit any（`checked` パラメータ等）が残存 |
| T4 | **エラーバウンダリが未実装** | React の Error Boundary がなく、コンポーネントのクラッシュが画面全体に波及する |
| T5 | **Lintルールが緩い** | ESLint設定が最小限で、unused imports や型安全性のチェックが弱い |

## 6. セキュリティ / 運用

| # | 課題 | 詳細 |
|---|------|------|
| S1 | **入力サニタイズが不完全** | ユーザー入力のXSS対策がReactの自動エスケープに依存。APIルートでの入力検証が不十分な箇所がある |
| S2 | **レート制限がない** | APIエンドポイントにレート制限がなく、不正アクセス時の防御が弱い |
| S3 | **CSRFトークンがない** | フォーム送信時のCSRF対策が見当たらない |
| S4 | **ログ・監視が限定的** | エラーログは console.error のみ。Sentry等の外部モニタリング未導入 |
| S5 | **環境変数のバリデーションがない** | 必須環境変数の存在チェック・型検証が起動時にされていない |

---

## 優先度の目安

- **高**: A1, A2, U1, U10, U12, P1, P5, T1, T4
- **中**: A4, U2, U3, U8, U11, F1, F4, F8, P2, T3, S1
- **低**: A5, A6, U4-U7, U9, F2, F3, F5-F7, F9, F10, P3, P4, T2, T5, S2-S5
