{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'set -a; source \"$(git rev-parse --show-toplevel)/.env.local\" 2>/dev/null; set +a; curl -s --form-string \"token=$PUSHOVER_APP_TOKEN\" --form-string \"user=$PUSHOVER_USER_KEY\" --form-string \"message=Claude が作業を完了しました\" --form-string \"title=Claude Code\" https://api.pushover.net/1/messages.json > /dev/null'"
          }
        ]
      }
    ]
  }
}
