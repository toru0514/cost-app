# UI改善案（カード見え解消後の可読性改善）

## 現状の課題
- 「カード見え」を消すために、`globals.css` で広範囲に `!important` 上書きを入れている
- セクションの区切りが弱く、どこからどこまでが1ブロックか分かりづらい
- テーブル・ツールバー・見出しの余白バランスが崩れ、情報密度が高すぎる

## 方針案

### 案A（最小修正）
- 既存の上書きを維持
- 各セクションに「薄い区切り線 + 上下余白」だけ追加
- テーブルは現状のまま

メリット:
- 変更が最小で安全
- 短時間で改善可能

デメリット:
- 根本的な読みづらさは一部残る

---

### 案B（推奨: バランス型）
- `dashboard-table-style` の強い全体上書きを縮小
- セクション単位でレイアウトを明示
  - 見出しエリア: 余白を確保
  - ツールバー: `rounded-md border-dashed` で軽く区切る
  - テーブル: `rounded-lg border` を維持（ヘッダーはグレー）
- 「カード」は使わないが、読みやすい情報ブロックを再構成

メリット:
- 「カード見え」回避と可読性を両立
- デザイン方針（テーブル中心）と整合

デメリット:
- 対象ファイルがやや多い

---

### 案C（テーブル特化）
- セクション外の装飾をほぼ排除
- テーブルのみを主役にし、ツールバーも1行に寄せる
- 余白を詰めて業務画面寄りにする

メリット:
- 画面効率が高い
- 一貫性が非常に強い

デメリット:
- 好みが分かれる
- 現状より無機質に見える可能性

## 実施提案
- まず **案B** で `/cost` と `/master` を先行調整
- 見た目合意後に `/analytics` `/bulk` `/audit` `/product` へ横展開

## 確認ポイント（レビュー観点）
- セクション境界が認識しやすいか
- ヘッダー > ツールバー > テーブルの視線誘導が自然か
- 行ホバーアクションの視認性が十分か
- モバイルで崩れないか
