# 時間計測ページ 実装計画

## Context
原価管理アプリに、作業時間を計測するストップウォッチ機能ページを新規追加する。マスタ登録の人件費や商品の制作時間の参考値として使用する。計測した時間は後から編集可能にする。

## 方針
- **データ保存**: AppData に統合（Supabase同期あり）
- **ページ配置**: DashboardPageとは独立した `/timetrack` ページ
- **データ連携**: まずは参考値のみ（将来的に商品データ連携を追加可能）

---

## Step 1: 型定義の追加

**ファイル**: `src/lib/types/time-record.ts`（新規）

```typescript
export type TimeRecordLap = {
  id: string
  label: string         // ラップ名（例: "ラップ 1"）
  duration: number      // ミリ秒
}

export type TimeRecord = {
  id: string
  taskName: string      // 作業名（例: "梱包作業"）
  totalDuration: number // 合計時間（ミリ秒）
  laps: TimeRecordLap[]
  note?: string
  createdAt: string     // ISO 8601
  updatedAt: string     // ISO 8601
}
```

**ファイル**: `src/lib/types/index.ts` — `TimeRecord`, `TimeRecordLap` のエクスポート追加

**ファイル**: `src/lib/types/app-data.ts` — `AppData` に `timeRecords: TimeRecord[]` を追加、`emptyAppData` に `timeRecords: []` を追加

## Step 2: CRUD アクション追加

**ファイル**: `src/lib/app-data/crud-actions.ts`

既存パターンに沿って以下を追加:
- `addTimeRecord(input)` — 新規記録追加
- `updateTimeRecord(input)` — 記録の編集（時間・タスク名・ラップ・メモ）
- `removeTimeRecord(id)` — 記録の削除
- `bulkRemoveTimeRecords(ids)` — 一括削除

返却オブジェクトにこれらを追加。

## Step 3: データ永続化対応

**ファイル**: `src/lib/app-data/use-persistence.ts`（または同等のsyncロジック）

- `timeRecords` の Supabase テーブルへの同期マッピングを追加
- 既存の `sync_app_data` パターンに従う

**ファイル**: Supabaseマイグレーション（必要に応じて）
- `time_records` テーブル作成（user_id, id, task_name, total_duration, laps (jsonb), note, created_at, updated_at）

## Step 4: 独立ページ作成

**ファイル**: `src/app/timetrack/page.tsx`（新規）

独立ページとして作成。DashboardPage は使わず、独自レイアウト。
- サーバーコンポーネントで `getServerAppData()` を呼び出し
- クライアントコンポーネント `TimeTrackPage` をラップ

```
src/app/timetrack/
  page.tsx              # サーバーコンポーネント（エントリーポイント）
  timetrack-page.tsx    # クライアントコンポーネント（メインUI）
```

## Step 5: UIコンポーネント作成

**ファイル**: `src/app/_components/timetrack/`（新規ディレクトリ）

### 5a. ストップウォッチコンポーネント
**ファイル**: `stopwatch.tsx`

- 大きなデジタル時計表示（HH:MM:SS.ss）
- 操作ボタン: 開始、一時停止、再開、停止、ラップ
- `useRef` + `requestAnimationFrame` or `setInterval` でタイマー管理
- ラップボタン押下でラップ記録をリアルタイム追加

### 5b. ラップ一覧コンポーネント
**ファイル**: `lap-list.tsx`

- ラップ番号、ラップタイム、合計タイム表示
- テーブル形式

### 5c. タスク入力 + 保存フォーム
**ファイル**: `task-form.tsx`

- タスク名入力（"〜作業" のプレースホルダー）
- メモ入力（任意）
- ストップウォッチ停止後に「保存」ボタンで記録を AppData に保存

### 5d. 記録履歴一覧
**ファイル**: `record-history.tsx`

- 過去の計測記録一覧（テーブル形式）
- 作業名、合計時間、ラップ数、日時を表示
- 検索・ソート機能
- 各記録の編集・削除ボタン

### 5e. 記録編集ダイアログ
**ファイル**: `edit-record-dialog.tsx`

- ダイアログで記録を編集
- タスク名、各ラップの時間（HH:MM:SS入力）、メモを変更可能
- 合計時間はラップの合計から自動計算

## Step 6: ナビゲーション追加

**ファイル**: `src/app/_components/shared/sidebar.tsx`

- サイドバーにリンクを追加（アイコン: `Timer` from lucide-react）
- タブではなく外部リンクとして `/timetrack` へ遷移

## Step 7: timetrack ページのレイアウト

`timetrack-page.tsx` のレイアウト:
- ヘッダー（← 戻るリンク + "時間計測" タイトル）
- 左上: タスク名入力
- 中央: ストップウォッチ表示 + 操作ボタン
- 下部: ラップ一覧（計測中）
- 下部: 記録履歴一覧（過去の記録）

---

## 修正対象ファイル一覧

| ファイル | 変更内容 |
|---------|---------|
| `src/lib/types/time-record.ts` | **新規** 型定義 |
| `src/lib/types/index.ts` | エクスポート追加 |
| `src/lib/types/app-data.ts` | `timeRecords` フィールド追加 |
| `src/lib/types/sample-data.ts` | `timeRecords: []` 追加 |
| `src/lib/app-data/crud-actions.ts` | CRUD関数追加 |
| `src/lib/app-data/use-persistence.ts` | 同期マッピング追加 |
| `src/app/timetrack/page.tsx` | **新規** サーバーコンポーネント |
| `src/app/timetrack/timetrack-page.tsx` | **新規** クライアントページ |
| `src/app/_components/timetrack/stopwatch.tsx` | **新規** ストップウォッチ |
| `src/app/_components/timetrack/lap-list.tsx` | **新規** ラップ一覧 |
| `src/app/_components/timetrack/task-form.tsx` | **新規** タスク入力 |
| `src/app/_components/timetrack/record-history.tsx` | **新規** 記録履歴 |
| `src/app/_components/timetrack/edit-record-dialog.tsx` | **新規** 編集ダイアログ |
| `src/app/_components/shared/sidebar.tsx` | ナビリンク追加 |

## 再利用する既存リソース

- `src/components/ui/` — shadcn/ui コンポーネント（Button, Card, Dialog, Input, Table）
- `src/lib/app-data/crud-actions.ts` — CRUD パターン（`createId`, `update` 関数）
- `src/lib/app-data/use-persistence.ts` — Supabase同期パターン
- `lucide-react` — Timer アイコン

## 検証方法

1. `/timetrack` にアクセスして時間計測ページが表示されることを確認
2. タスク名を入力し、ストップウォッチを開始・ラップ・停止できることを確認
3. 停止後に記録を保存し、履歴一覧に表示されることを確認
4. 記録の編集ダイアログで時間を手動変更できることを確認
5. 記録を削除できることを確認
6. サイドバーから `/timetrack` に遷移できることを確認
7. `npm run build` が成功することを確認
