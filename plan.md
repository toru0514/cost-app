# 集計データページ 表示項目拡充計画

## 現状分析

現在の集計データページ（`/analytics`）は以下5セクションで構成されている:

| セクション | 表示内容 |
|-----------|---------|
| フィルターパネル | 期間選択（3/6/12/24ヶ月）、大カテゴリフィルター |
| 期間比較カード | 合計コスト、平均単価、商品数、前期比差分（金額・%） |
| カテゴリ別ランキング | 大/中/小カテゴリごとのコストランキング（上位6件）|
| コスト構成グラフ | 9コストカテゴリの構成比（積み上げバー＋凡例） |
| 月次トレンド | 月ごとの合計コスト推移（横棒グラフ） |

## 追加可能な項目（データが既に存在するもの）

以下は、既存のデータモデル・計算ロジックから **追加のDB変更なしに** 表示できる項目である。

### 1. 利益・粗利セクション（新規セクション）
- **根拠**: `product.salePrice` が全商品に存在し、`costs.total` との差分で粗利が計算可能
- **表示項目**:
  - 粗利合計（売上合計 − コスト合計）
  - 粗利率（粗利 / 売上 × 100）
  - 前期比の粗利差分
- **実装**: `ProfitSummaryCard` コンポーネントを新規追加
- **優先度**: **高** — コスト管理アプリにおいて粗利は最重要指標の一つ

### 2. 商品別コストランキング（新規セクション）
- **根拠**: `ProductSummary` に商品名・コスト情報が全て含まれている
- **表示項目**:
  - コスト上位商品ランキング（上位10件）
  - 商品名、単価、合計コスト、カテゴリ名
- **実装**: `ProductRankingSection` コンポーネントを新規追加
- **優先度**: **高** — カテゴリ別はあるが商品単位の比較がない

### 3. コスト構成の前期比較（既存セクション拡張）
- **根拠**: `previousCostTotals` が既に計算済みだが、コスト構成セクションでは使われていない
- **表示項目**:
  - 各コストカテゴリの前期比増減（金額・%）
  - どのコスト項目が増加/減少したかの可視化
- **実装**: `CostCompositionSection` に前期データを渡して差分表示を追加
- **優先度**: **高** — データは既にあり、少量の変更で実現可能

### 4. 平均単価トレンド（既存セクション拡張）
- **根拠**: 月次トレンドは合計コストのみ表示。`quantity` データが既にあるため平均単価の推移も計算可能
- **表示項目**:
  - 月ごとの平均単価推移
  - 合計コストと平均単価の切り替え表示
- **実装**: `MonthlyTrendSection` に平均単価データを追加、表示切替ボタンを設置
- **優先度**: **中** — 合計だけでなく単価の傾向も分析に有用

### 5. ステータス別集計（新規セクション）
- **根拠**: `product.status`（draft / active / discontinued）が存在するが集計ページで未使用
- **表示項目**:
  - ステータスごとの商品数・コスト合計
  - 円グラフまたは簡易カード形式
- **実装**: `StatusBreakdownCard` コンポーネントを新規追加
- **優先度**: **中** — 運用状態の把握に有用

### 6. コストカテゴリ別月次トレンド（既存セクション拡張）
- **根拠**: 月次トレンドは total のみ。カテゴリ別の内訳データは既に `filteredEntries` から計算可能
- **表示項目**:
  - 9カテゴリ別の月次推移（積み上げ棒グラフ）
  - 特定カテゴリの選択フィルター
- **実装**: `MonthlyTrendSection` の拡張、またはサブセクション追加
- **優先度**: **中** — どのコスト項目が時系列で変動しているか把握可能

## 実装計画

優先度「高」の3項目を第1フェーズとして実装する。

### Step 1: 利益・粗利セクション追加
- `src/app/_components/analytics/sections/profit-summary-card.tsx` を新規作成
- `analytics-tab.tsx` の `ProductSummary` 集計に `salePrice × quantity` の売上合計を追加
- `utils.ts` の `CostTotals` 型に `totalRevenue` フィールドを追加
- 期間比較カードの下に配置

### Step 2: 商品別コストランキング追加
- `src/app/_components/analytics/sections/product-ranking-section.tsx` を新規作成
- `filteredEntries` から商品ごとの合計コスト・単価を算出し、上位10件を表示
- カテゴリランキングの下に配置

### Step 3: コスト構成の前期比較追加
- `CostCompositionSection` に `previousTotals` props を追加
- 各コストカテゴリの凡例に前期比の増減（金額・%）を並記
- 増加は赤系、減少は緑系で色分け

### Step 4: テスト・動作確認
- 既存テストが通ることを確認
- ビルドが成功することを確認
