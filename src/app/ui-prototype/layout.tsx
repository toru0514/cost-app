import { PrototypeShell } from "./_components/prototype-shell"

export default function UiPrototypeLayout({ children }: { children: React.ReactNode }) {
  return <PrototypeShell>{children}</PrototypeShell>
}
